on: [push, pull_request, workflow_dispatch]

name: Build ZMK Firmware

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    uses: SethMilliken/zmk/.github/workflows/build-user-config.yml@zen-display-patches
